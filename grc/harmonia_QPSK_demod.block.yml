id: harmonia_QPSK_demod
label: QPSK_demod
category: '[harmonia]'

parameters:
- id: sps
  label: Samples per Symbol
  dtype: int
  default: "4"

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
  imports: from gnuradio import harmonia
  make: |-
    harmonia.QPSK_demod(${sps})

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
