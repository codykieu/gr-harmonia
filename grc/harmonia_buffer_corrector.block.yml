id: harmonia_buffer_corrector
label: Buffer Corrector
category: '[harmonia]'

parameters:
- id: max_buffer_size
  label: Max Buffer Size
  dtype: int
  default: "1996"

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
    
templates:
  imports: from gnuradio import harmonia
  make: harmonia.buffer_corrector(${max_buffer_size})

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
