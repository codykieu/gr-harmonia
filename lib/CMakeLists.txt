# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-harmonia
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX
list(APPEND harmonia_sources
    pdu_complex_to_mag_impl.cc
    pdu_fft_impl.cc
    device.cc
    frequency_pk_est_impl.cc
    single_tone_src_impl.cc
    usrp_radar_2_impl.cc
    SDR_tagger_impl.cc
    usrp_radar_all_impl.cc
    usrp_radar_tx_impl.cc
    usrp_radar_rx_impl.cc
    clock_drift_est_impl.cc
    time_pk_est_impl.cc
    buffer_corrector_impl.cc
    clockbias_phase_est_impl.cc
    QPSK_mod_impl.cc
    QPSK_demod_impl.cc
    LFM_src_impl.cc
    compensation_impl.cc )

set(harmonia_sources
    "${harmonia_sources}"
    PARENT_SCOPE)
if(NOT harmonia_sources)
    message(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT harmonia_sources)

add_library(gnuradio-harmonia SHARED ${harmonia_sources})

target_link_libraries(gnuradio-harmonia 
    PUBLIC
    gnuradio::gnuradio-runtime
    gnuradio::gnuradio-blocks
    Qt5::Widgets
    qwt::qwt
    Python::Python
    ${UHD_LIBRARIES}
    nlohmann_json::nlohmann_json
    plasma_dsp
    ArrayFire::af)

target_include_directories(
    gnuradio-harmonia
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
    )
set_target_properties(gnuradio-harmonia PROPERTIES DEFINE_SYMBOL "gnuradio_harmonia_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-harmonia PROPERTIES INSTALL_NAME_DIR
                                                    "${CMAKE_INSTALL_PREFIX}/lib")
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
gr_library_foo(gnuradio-harmonia)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here

list(APPEND test_harmonia_sources
qa_device.cc
)

# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-harmonia)

if(NOT test_harmonia_sources)
    message(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_harmonia_sources)

foreach(qa_file ${test_harmonia_sources})
    gr_add_cpp_test("harmonia_${qa_file}" ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file})
endforeach(qa_file)
